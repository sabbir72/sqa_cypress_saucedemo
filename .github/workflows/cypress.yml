# name: Cypress E2E Tests

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   cypress-run:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 18

#       - name: Install dependencies
#         run: npm install

#       - name: Run Cypress Tests
#         uses: cypress-io/github-action@v6
#         with:
#           browser: chrome
#           headless: true

#       - name: Generate Allure Report
#         run: |
#           npm install -g allure-commandline
#           allure generate allure-results --clean -o allure-report

#       - name: Upload Allure Report Artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: allure-report
#           path: allure-report

# ==================================================================

# name: Cypress E2E Tests & Allure Publish

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   cypress-test:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 18

#       - name: Install dependencies
#         run: npm install

#       - name: Run Cypress Tests
#         uses: cypress-io/github-action@v6
#         with:
#           browser: chrome
#           headless: true

#       - name: Generate Allure Report
#         run: |
#           npm install -g allure-commandline
#           allure generate allure-results --clean -o allure-report

#       - name: Upload Allure Report Artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: allure-report
#           path: allure-report

#   publish-report:
#     needs: cypress-test
#     runs-on: ubuntu-latest
#     permissions:
#       pages: write
#       id-token: write

#     steps:
#       - name: Download Allure Report Artifact
#         uses: actions/download-artifact@v4
#         with:
#           name: allure-report
#           path: ./allure-report

#       - name: Upload GitHub Pages artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: ./allure-report

#   deploy:
#     needs: publish-report
#     runs-on: ubuntu-latest
#     permissions:
#       pages: write
#       id-token: write

#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}

#     steps:
#       - id: deployment
#         uses: actions/deploy-pages@v4

# =================================================================
name: Run Cypress & Deploy Allure Report

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Run Cypress Tests
        run: |
          npx cypress run --env allure=true

      - name: Generate Allure Report
        run: |
          npx allure generate allure-results --clean -o allure-report

      - name: Upload GitHub Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./allure-report   # MUST be only one artifact

  deploy:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
